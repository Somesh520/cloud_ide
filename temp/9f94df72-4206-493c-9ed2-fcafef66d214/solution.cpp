#include <iostream>
using namespace std;

int main() {
    int a, b;
    // Reading input from the test cases
    if (!(cin >> a >> b)) return 0;
    
    // Outputting the sum for the test case to verify
    cout << a + b;
    
    return 0;
}
